# 🛠️ かんトレ 今後のリファクタリング提案・設計方針書

## 1. 概要と目的

直近の改修により、起動時のオーディオアンロック、レイアウトシフトの防止、クラウド進捗同期の型ガードなどの機能要件は安定稼働を達成しました[cite: 1, 8, 11, 16]。  
一方で、短期間での度重なるピンポイント修正の結果、**特定のモジュールへの責務集中（神クラス化）**、**進捗データの二重帳簿化**、**共通UI処理の重複** が発生しています[cite: 5, 7, 8, 14]。

本ドキュメントは、今後予定される「新学年（6年生等）のデータ追加」や「新規機能開発」を安全かつスムーズに進めるための、リファクタリング対象箇所および改善アーキテクチャの指針をまとめたものです[cite: 1, 8]。

---

## 2. 課題の整理と改善方針

### ① `main.js` の神クラス化と責務過密

* **現状の課題**:
  * `KanjiApp` クラスが、アプリ全体の起動・ルーティング、練習セッションの状態管理（問題インデックス、文字タブ切り替え、`userInputs` 配列の保持）、リアルタイムプレビュー更新、解答判定呼び出し、ログ生成・送信までを一手に引き受けています[cite: 8]。
  * `main.js` が約600行規模に達しており、画面遷移の修正時にも手書き描画の内部ステートに影響を及ぼしやすい構造になっています[cite: 8]。
* **改善方針**:
  * **「練習セッション管理（`PracticeSession` / `QuestionController`）」の抽出**:
    * 現在の問題、文字インデックス、下部プレビューの管理、判定処理（`validator` 連携）を独立モジュールへ切り出します[cite: 8, 10]。
  * **`KanjiApp` の薄型化（オーケストレーター化）**:
    * `main.js` は、認証（`auth.js`）、メニュー（`menu.js`）、練習（`practice`）、特訓（`drill.js`）、挑戦状（`challenge.js`）の各マネージャー間のイベント中継と画面ルーティングのみを担当させます[cite: 5, 8, 12, 14, 15]。

---

### ② 進捗データ管理（`storage.js` / `auth.js` / `menu.js`）の二重帳簿

* **現状の課題**:
  * `clearedSets`（クリア済み単元）が、`Storage` 内では連想配列（`{"1学期_01": "ISO日時"}`）、`AuthManager` 内ではキー配列（`["1学期_01"]`）、`MenuManager` 内でも独自配列としてそれぞれ別個にメモリ保持されています[cite: 5, 7, 12]。
  * その結果、型変換を行うヘルパー関数（`_normalizeClearedSets` や `normalizeClearedSetsPayload` 等）が複数モジュールに重複定義され、GAS通信時の型事故や同期ズレの温床となっています[cite: 4, 5, 7]。
* **改善方針**:
  * **単一の信頼できる情報源（Single Source of Truth: `ProgressStore`）の確立**:
    * 進捗（クリア状況、苦手文字統計、日次フラグ）の状態管理を `storage.js` に完全集約します[cite: 7]。
    * 内部データ構造は「連想配列形式」に統一し、他モジュールは独自のコピー変数を保持せず、常にストアのメソッド（`store.isCleared(setId)`、`store.getClearedList()` 等）を参照する形に改めます[cite: 5, 7, 12]。

---

### ③ 通常練習と特訓モード（`drill.js`）のUI・描画処理の重複

* **現状の課題**:
  * `main.js` の通常練習画面と `drill.js` の1文字特訓画面で、それぞれ個別に `CanvasController` の生成、アンドゥ/リドゥ/消しゴムボタンの活性制御、2行画数表示の更新処理が重複して実装されています[cite: 8, 13, 14]。
  * 問題データからの画数検索（`_lookupStrokeCount`）や、単一文字の正誤判定ペイロードの組み立て処理も両者で別々に書かれています[cite: 8, 10, 14]。
* **改善方針**:
  * **「単一文字手書きコンポーネント（`SingleCharCanvas`）」の共通化**:
    * 260pxキャンバス、操作ボタン（1画戻る/進む/消去）、画数表示パネル（`stroke-info`）を1つの再利用可能なクラスにまとめます[cite: 2, 13, 14, 21]。
    * 通常練習画面のタブ切り替え時、および特訓画面の練習カードの双方からこのコンポーネントを使い回すことで、重複コードを大幅に削減します[cite: 8, 14]。

---

### ④ 画面遷移制御とDOM直接操作の散在

* **現状の課題**:
  * `main.js`、`auth.js`、`drill.js`、`ui.js` の各クラスで `document.getElementById` が個別に呼ばれ、`style.display = 'none'` や `'flex'` が各所で直接書き換えられています[cite: 8, 9, 12, 14]。
  * 画面数（ローディング、ログイン、メニュー、練習、クリア、特訓一覧、特訓練習、挑戦状）が増加したことで、どの画面が開いているときにどの画面を閉じるべきかの網羅的な把握が難しくなっています[cite: 2, 8, 14]。
* **改善方針**:
  * **画面状態管理（スクリーンルーター）の導入**:
    * `ui.js` に `setScreen(screenName)` メソッドを設け、画面の表示/非表示（`menu`、`practice`、`drill-list`、`drill-practice`、`all-clear`）を一括管理します[cite: 8, 9, 14]。
    * 各モジュールが他の画面のDOMを直接 `display: none` するのを禁止し、状態遷移を一箇所で可視化します[cite: 8, 9, 14]。

---

## 3. リファクタリング実施ステップと優先順位

段階的に安全性を保ちながら進めるための推奨ロードマップです[cite: 1]。

| 優先度 | 対象タスク | 主な変更ファイル | リスク | 期待される効果 |
| :--- | :--- | :--- | :--- | :--- |
| **高（Phase 1）** | **進捗データ型の一元化** | `storage.js`, `logger.js`, `auth.js`[cite: 4, 7, 12] | 低 | スプレッドシート破損リスクの恒久排除、型変換重複コードの削除[cite: 1, 4] |
| **中（Phase 2）** | **練習セッションの分離** | `main.js`, 新規 `session.js`[cite: 8] | 中 | `main.js` の肥大化解消（約300行削減）、練習進行バグの局所化[cite: 8] |
| **中（Phase 3）** | **手書きUIコンポーネント共通化** | `canvas.js`, `drill.js`, `main.js`[cite: 8, 13, 14] | 低〜中 | 通常練習と特訓画面の操作感の完全統一、保守工数削減[cite: 8, 14] |
| **低（Phase 4）** | **画面ルーターの統合** | `ui.js`, `main.js`[cite: 8, 9] | 低 | 画面の重なり・非表示漏れバグの完全撲滅[cite: 2, 8] |

---

## 4. 開発規約との整合性（注意事項）

リファクタリングを実施する際も、HANDOVERの最重要開発規約を遵守します[cite: 1]。

1. **通信プロトコル・データフォーマットの不可侵**:
   * GAS Web API との通信仕様（`Content-Type: text/plain;charset=utf-8`、アクション名、返却JSON形式）は1文字も変更しない[cite: 1, 4]。
2. **Google Input Tools API 仕様の維持**:
   * `validator.js` 内のインク座標配列の形式 `[[x...], [y...]]` は実績のあるコードをそのまま維持する[cite: 1, 10]。
3. **Vanilla ES Modules の維持**:
   * 外部バンドラー（Webpack/Vite等）を導入せず、標準の `import` / `export` で疎結合に分割する[cite: 1]。