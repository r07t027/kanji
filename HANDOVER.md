```markdown
# 📖 漢字練習Webアプリ システム設計・保守・データ定義完全ガイド（HANDOVER v3）

## 1. 全体構造とアーキテクチャ方針

本プロジェクトは、小学校児童向けのタブレット（Chromebook / iPad 等）およびPC環境に最適化された **漢字手書き練習Webアプリケーション** です。
外部ビルドツール（Webpack / Vite 等）を一切使用せず、ブラウザ標準の **ES Modules (`import` / `export`)** による疎結合なモジュール設計を採用しています。

### 核心設計思想

1. **Chromebook（1366×768）完全対応（スクロールゼロ設計）**:
   * アドレスバーやシェルフによる実効表示領域（約550〜600px）を考慮し、全体を `height: 100vh; overflow: hidden;` で固定。
   * 手書きキャンバス（260×260px）と上下配置の正誤判定カード（72×72px）により、一切の縦スクロールを発生させません。
2. **ユニバーサルデザイン・インクルーシブ対応（利き手モード）**:
   * メニュー画面で「ひだりきき（左配置）」「みぎきき（右配置）」を選択可能。
   * 左利き時は CSS の `flex-direction: row-reverse;` により、手書き描画領域を画面左側、問題提示を画面右側へと瞬時に反転。書く手で問題文が隠れるのを防ぎます。
   * ユーザー情報としてスプレッドシートに保持され、ログイン時に自動適用されます。
3. **ゼロ秒レスポンス UX（事前先読み / プリフェッチ）**:
   * 画面表示時は静的 JSON (`data/users.json`) によりミリ秒単位でクラス・名前プルダウンを描画。
   * 操作中の数秒間にバックグラウンドで Google Apps Script (GAS) 経由で認証情報・進捗サマリーを非同期先読み。ログインボタン押下時は通信待ちゼロ（0ミリ秒）で即時認証します。
   * `localStorage` によるセッション保持を備え、1人1台端末では2回目以降のログイン入力を完全スキップします。
4. **引き継ぎ・異動に強い「2ファイル分離型」クラウドデータ基盤**:
   * GAS 経由で Google ドライブ上の 2 つのスプレッドシートへアクセス。
   * **マスター・進捗ファイル**: 児童数分の固定行（1人1行）。ログイン照合・💮クリアマーク・苦手漢字サマリーを高速読み書き。
   * **詳細ログファイル**: 追記専用（Append-only）。1問ごとのストローク数・誤答文字・タイムスタンプを蓄積。画面表示からは一切読み出さないため、ログが数万行に膨らんでも日常利用の速度低下が起きません。
5. **教育的配慮に基づいたUI・フィードバック**:
   * 文部科学省・筆順指導要領および教科書体に準拠した **KanjiVG公式SVGデータ** を描画・筆順アニメーション再生。
   * Google Input Tools 手書き認識 API ＋ 画数判定による二重チェック。

---

### ディレクトリ構成

```text
kanji_practice_app/
│
├── index.html                    # ログインモーダル / メニュー / 練習の骨格DOM
├── css/
│   └── style.css                 # 2カラムレイアウト・利き手反転・ログインUI・バッジ
├── data/
│   ├── users.json                # 静的児童名簿（クラス・出席番号・ひらがな名・userId）※PIN非保持
│   └── grade5_questions.json     # 小学5年生 全44回（計220問・各5問）構造化データ
├── assets/
│   └── audio/                    # 効果音アセット (MP3)
│       ├── correct.mp3           # 正解音 (ピンポン♪)
│       ├── wrong.mp3             # 不正解音 (ブブー)
│       └── complete.mp3          # 全問クリア音 (ファンファーレ)
└── js/
    ├── main.js                   # アプリ統括・認証フロー・単元切替・統合判定
    ├── canvas.js                 # 260px手書き描画・アンドゥ/リドゥ・ストローク管理
    ├── recognition.js            # Google Input Tools 手書き文字認識 API 連携
    ├── audio.js                  # Web Audio API によるプリロード & 自動再生ガード
    ├── kanjivg.js                # 教科書体SVG直接描画 & 筆順再生
    ├── ui.js                     # メニュー/練習/クリア画面DOM描画・利き手制御
    └── logger.js                 # GAS Web API 通信（事前先読み・進捗更新・詳細ログ保存）

```

---

## 2. クラウドデータ仕様（Google スプレッドシート ＆ GAS）

### 1. スプレッドシート構成

* **ファイルA: `漢字アプリ_ユーザー・進捗マスター**`
* `users` タブ: 名簿・PIN・設定情報
* `userId` | `className` | `studentNo` | `kanaName` | `pin` | `handMode`


* `progress` タブ: 画面表示用サマリー（1児童1行）
* `userId` | `clearedSets` (JSON配列) | `weakChars` (JSON連想配列) | `lastLogin`




* **ファイルB: `漢字アプリ_詳細ログ**`
* `logs` タブ: 書き込み専用履歴（1学習1行）
* `timestamp` | `userId` | `setId` | `qIndex` | `isSuccess` | `detailJson`





### 2. GAS Web API (`backend.gs`) エンドポイント機能

* `prefetchAllData`: 起動時の事前先読み用。`users` と `progress` を結合した連想配列を返却。
* `saveProgressAndLog`: 単元クリア時に `progress` タブの1行上書き更新 ＋ `logs` タブへの一括追記をアトミックに実行。

> **デプロイ時のお作法**:
> コードを変更した際は、必ず **「デプロイを管理」➔「鉛筆アイコン」➔「新バージョン」** を選択してデプロイしてください。アクセスできるユーザーは必ず **「全員（Anyone）」** に設定します。

---

## 3. クライアント側 モジュール別 役割分担

### 1. `js/main.js`（アプリケーション統括）

* 起動時に `prefetchAllDataAsync()` をバックグラウンド実行し、同時に `data/users.json` からログインモーダルを描画。
* ローカルストレージにセッション（`kanji_current_user`）があれば自動ログイン。
* 単元クリア時にクリア済みリスト（`clearedSets`）を更新し、メニューの単元ボタンに「💮」バッジを描画。
* 学習セッションログを蓄積し、全問完了時に `saveProgressAndLogs()` を呼び出し。

### 2. `js/logger.js`（通信ハブ）

* GAS の 302 リダイレクトに対応した `redirect: 'follow'` による POST 通信。
* `fetchClassAndUsersFromLocal()`: 初回0秒描画用の静的JSONフェッチ。
* `prefetchAllDataAsync()`: スプレッドシート全データの非同期プリフェッチ。
* `saveProgressAndLogs()`: 進捗と詳細ログの二重保存リクエスト。

### 3. `js/canvas.js`（描画・ストローク）

* 260×260px のマス目描画。アンドゥ・リドゥ（1画ずつ復元）およびキーボードショートカット対応。

### 4. `js/recognition.js`（OCR）

* Google Input Tools API にストローク配列を送信し、上位候補を抽出。

### 5. `js/kanjivg.js`（お手本・筆順）

* 正解時は静止画、不正解時はインタラクティブ（タップでアニメーション再生）なお手本を表示。

### 6. `js/ui.js`（表示レイアウト）

* 利き手反転クラス付与、上下比較カード、メッセージエリアの動的書き換え。

---

## 4. 今後の開発・拡張ロードマップ

* [ ] **学習データの可視化（ダッシュボード画面）**:
* 先生用（または児童用）の「がくしゅうきろく」画面を新設。
* `progress` の苦手文字（`weakChars`）や `logs` の正答率推移をグラフ・一覧表示。


* [ ] **学年の拡充**:
* `data/grade1_questions.json` 〜 `data/grade6_questions.json` の追加。


* [ ] **パスワード変更機能**:
* 児童自身が4桁暗証番号を変更できるUIとGAS更新アクションの追加。


* [ ] **検証用UIのクリーンアップ**:
* 動作確認用に追加したメニュー下部の「OCR判定の許容候補順位プルダウン」ブロックの撤去（または管理者モード化）。



```
