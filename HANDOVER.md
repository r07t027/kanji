# 📖 漢字練習Webアプリ「書きトレ」システム設計・保守・データ定義完全ガイド（HANDOVER v5）

## 1. アプリケーション概要と設計思想

本プロジェクトは、小学校児童向けのタブレット（Chromebook / iPad 等）およびPC環境に最適化された **漢字手書き練習Webアプリケーション「書きトレ」** です。
外部ビルドツール（Webpack / Vite 等）を介さず、ブラウザ標準の **ES Modules (`import` / `export`)** による疎結合なモジュール設計を採用しています。

### 核心設計思想

1. **Chromebook（1366×768）完全対応（スクロールゼロ設計）**:
* アドレスバーやシェルフによる実効表示領域（約550〜600px）を前提とし、全体を `height: 100vh; overflow: hidden;` で固定。


* 手書きキャンバス（250×250px）、プレビュー枠（52×52px）、上下判定カード（72×72px）により、縦スクロールを一切発生させません。




2. **インクルーシブ対応（ききてモードのオンデマンド切替）**:
* 右上メニューの「🤚 ききて」ボタン、または初回ログイン時の自動ポップアップで設定可能。


* CSS の `flex-direction: row-reverse;` により、手書き描画領域を画面左側、問題提示を画面右側へと瞬時に反転し、手で問題文が隠れるのを防止。


* クラウド（GAS `users`シート）およびローカルキャッシュへ即時保存され、自動復元。




3. **ゼロ秒レスポンス UX（事前先読み / プリフェッチ）**:
* 起動時は静的 JSON (`data/users.json`) によりミリ秒単位でクラス・名前プルダウンを描画。


* 操作中の数秒間にバックグラウンドで Google Apps Script (GAS) 経由で認証情報・進捗サマリーを非同期先読み。ログイン押下時は通信待ちゼロ（0ミリ秒）で即時認証。


* `localStorage` によるセッション保持を備え、2回目以降はログインモーダルを自動スキップ。




4. **引き継ぎ・異動に強い「2ファイル分離型」クラウドデータ基盤**:
* **マスター・進捗ファイル**: 児童数分の固定行（1人1行）。認証照合・進捗サマリー・設定（ききて/PIN）を高速読み書き。


* **詳細ログファイル**: 追記専用（Append-only）。1問ごとのストローク数・誤答文字・タイムスタンプを蓄積。




5. **ユニバーサルデザイン ＆ 自己調整学習の促進**:
* 問題提示・ナビゲーション・マスコット発話を徹底的にひらがな・和語化し、解答すべき漢字が画面内に露出するカンニングを完全防止。
* 書いた文字が下部のプレビュー枠へリアルタイムにDataURL（画像）として同期・永続表示され、俯瞰確認が可能。




6. **達成感を高める「1画面フルスクリーン・クリア演出」**:
* 練習中の左右2カラム制約を全問クリア時に解除し、画面中央に迫力あるクリアカードとマスコットによる祝福を展開。



---

### ディレクトリ構成（v5 最新版）

CSSおよびJSの関心事を分離し、保守性と拡張性を大幅に向上させました。

```text
kanji_practice_app/
│
├── index.html                    # ログイン/各種モーダル/メニュー/練習/全問クリアDOM[cite: 15]
├── css/
│   ├── base.css                  # 全体リセット・100vh固定レイアウト・共通アニメーション
│   ├── modal.css                 # ログイン・ききて設定・パスワード変更モーダル
│   ├── menu.css                  # メニュー画面・学期タブ・単元ボタングリッド
│   ├── practice.css              # 練習画面2カラム・利き手反転・キャンバス・操作ボタン[cite: 11]
│   └── result.css                # 上下判定カード・筆順再生枠・1画面フルスクリーンクリア[cite: 12]
├── data/
│   ├── users.json                # 静的児童名簿（クラス・出席番号・ひらがな名・userId）[cite: 1]
│   └── grade5_questions.json     # 小学5年生 全44回（計220問・各5問）構造化データ[cite: 1]
├── assets/
│   ├── images/
│   │   ├── logo_01.png           # 「書きトレ」公式ブランドロゴ[cite: 1]
│   │   ├── kakimaru_01.png       # かきまる（案内・クリップボード）
│   │   ├── kakimaru_02.png       # かきまる（案内・両手広げ）
│   │   ├── kakimaru_03.png       # かきまる（正解・グッド）
│   │   ├── kakimaru_04.png       # かきまる（正解・ウインク）
│   │   ├── kakimaru_05.png       # かきまる（大正解/クリア・バンザイ）
│   │   ├── kakimaru_06.png       # かきまる（不正解/疑問・はてな）
│   │   ├── kakimaru_07.png       # かきまる（不正解・バツ札）
│   │   └── kakimaru_08.png       # かきまる（不正解・困り顔はてな）
│   └── audio/                    # 効果音アセット (MP3)[cite: 1, 4]
│       ├── correct.mp3           # 正解音 (ピンポン♪)[cite: 1, 4]
│       ├── wrong.mp3             # 不正解音 (ブブー)[cite: 1, 4]
│       └── complete.mp3          # 全問クリア音 (ファンファーレ)[cite: 1, 4]
└── js/
    ├── main.js                   # アプリ統括・進行制御・出題シャッフル・セリフ定義
    ├── auth.js                   # 認証・セッション・ききて/PIN変更モーダル制御
    ├── menu.js                   # メニュー画面・学期タブ・単元選択グリッド制御
    ├── validator.js              # Google Input Tools連携・画数判定・ログ生成
    ├── canvas.js                 # 250px手書き描画・アンドゥ/リドゥ・ストローク管理[cite: 1, 6]
    ├── recognition.js            # 手書き文字認識 API 連携（第2候補まで）[cite: 1, 8]
    ├── audio.js                  # Web Audio API によるプリロード & 自動再生ガード[cite: 1, 4]
    ├── kanjivg.js                # 教科書体SVG直接描画 & 筆順アニメーション再生[cite: 1, 5]
    ├── ui.js                     # 画面DOM描画・マスコット表情連動・ボタン活性制御
    └── logger.js                 # GAS Web API 通信（先読み・進捗保存・設定更新）[cite: 1, 7]

```

---

## 2. クラウドデータ仕様（Google スプレッドシート ＆ GAS）

### 1. スプレッドシート構成

* **ファイルA: `漢字アプリ_ユーザー・進捗マスター**`

* `users` シート: 名簿・PIN・設定情報


* A列: `userId` | B列: `className` | C列: `studentNo` | D列: `kanaName` | E列: `pin`（暗証番号4桁） | F列: `handMode`（`left` または `right`）




* `progress` シート: 画面表示用サマリー（1児童1行）


* A列: `userId` | B列: `clearedSets` (JSON配列) | C列: `weakChars` (JSON連想配列) | D列: `lastLogin` (更新日時)






* **ファイルB: `漢字アプリ_詳細ログ**`

* `logs` シート: 書き込み専用履歴（1問1行）


* A列: `timestamp` | B列: `userId` | C列: `setId` | D列: `qIndex` | E列: `isSuccess` | F列: `detailJson`






### 2. GAS Web API (`backend.gs`) エンドポイント

* `prefetchAllData`: 起動時の事前先読み用。`users` と `progress` を結合した連想配列を返却。


* `saveProgressAndLog`: 単元クリア時に `progress` タブの1行上書き更新 ＋ `logs` タブへの一括追記。


* `updateHandMode`: ききて設定を `users` シートの F列へ即時書き込み。


* `updatePin`: 変更された暗証番号を `users` シートの E列へ即時書き込み。



---

## 3. UI/UX 仕様詳細（v5 最新版）

### 1. ログイン画面

* クラス選択前は「なまえ」プルダウンが `.custom-select:disabled` により確実にグレーアウト。


* 入力欄は「① クラス」「② なまえ」「③ パスワード（すうじ4けた）」の3ステップ。



### 2. メニュー画面

* **ヘッダー**:
* 左側に「書きトレ」ロゴ。


* 右側にユーザー情報バーを配置。「5ねん 1くみ たろう さん」のようにクラス名もひらがな化・スペース挿入。ボタン表記は `[🤚 ききて]` `[🔑 パスワード]` `[ログアウト]`。




* **学年選択**: 「1. どの がくねんの かんじ？」➔ 選択肢表記は「5ねんせい」。


* **単元選択**: 「2. もんだいを えらぶ」。タブは「1がっき」「2がっき」「3がっき」（3学期はグレーアウト非活性）。


* **スタートボタン**: シンプルに「チャレンジする」表記。



### 3. 回答（練習）画面

* **出題シャッフル**:
* 毎回の単元開始時、登録された5問を Fisher-Yates アルゴリズムで完全ランダム順に出題。


* **左カラム（問題ペイン）**:
* 単元名表記は「1がっき その1」フォーマット。
* 「もんだい 1/5」表記を削除し、問題文の文頭に丸数字（①〜⑤）を付与。
* 送り仮名注意バッジは「おくりがなまで ぜんぶ かいてね！」とひらがな化。
* **マスコット「かきまる」連動**:
* 案内・書き直し・正解・不正解の感情に応じ、全8種のイラストから状況に即した画像をランダム選定して表情切り替え。
* 絵文字を使わず、適宜スペースを挟んだ和語・ひらがな中心の語りかけ（文末は「。」）。
* 判定中の不要な発話は廃止（ボタン側の「かくにん中...」で表現）。
* 送り仮名の2文字目以降は「書き終わったら『こたえあわせ』を押してね」など、誤答誘導を防ぐ専用のアドバイス体系に自動切り替え。




* **右カラム（描画ペイン）**:
* タブ表記は「1もじめ」「2もじめ」。
* 履歴ボタンは「1かくもどる」「1かくすすむ」とひらがな化。


* 画数状況表示を2行化：
* 漢字マス：「このじは ◯かく です。」「いまの かくすう：◯かく」
* 送り仮名の2文字目以降：画数情報を完全非表示（空白保持）。


* 3連操作ボタン：**`[まえのもじへ]`（青） ➔ `[かきなおす]`（橙） ➔ `[つぎのもじへ]`（青）** で進行ボタンの色を統一。


* プレビュー欄ラベルは「かいたもじ」。


* アクションボタングループ：**`[こたえあわせ]`（全幅緑） ＋ `[パス]`（有彩色パープル）** の2連配置。パス押下時は確認ダイアログを経て正解お手本を表示し、苦手文字として登録。




* **判定結果画面（上下比較カード）**:
* 正解お手本カードのタイトルを「せいかいの おてほん」に固定し、不正解・パス時にお手本文字の下部へ「タッチすると かきじゅんが みられるよ。」を配置。





### 4. 全問クリア画面（1画面フルスクリーン）

* 練習中の左右2カラム表示をフェードアウトさせ、画面全体を使った中央集中カードを表示。


* 大きなはなまるバッジ（💮）＋「はなまる満点！」タイトル ＋ かきまる（大喜びポーズ）が「全5問クリア！さいごまで よくがんばったね！」と語りかける統合演出。



---

## 4. 今後の開発・拡張ロードマップ

* [ ] **学習記録ダッシュボードの追加**:
* 児童向け「がくしゅうきろく」画面。`progress` の苦手文字（`weakChars`）や `logs` の推移表示。




* [ ] **他学年データ（1〜4年・6年）の拡充**:
* `data/gradeX_questions.json` の追加と学年プルダウン（「◯ねんせい」）の連携。




* [ ] **オフライン・PWA対応**:
* Service Worker によるオフラインキャッシュとホーム画面追加対応。