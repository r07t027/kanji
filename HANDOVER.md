```markdown
# 📖 漢字練習Webアプリ「書きトレ」システム設計・保守・データ定義完全ガイド（HANDOVER v4）

## 1. アプリケーション概要と設計思想

本プロジェクトは、小学校児童向けのタブレット（Chromebook / iPad 等）およびPC環境に最適化された **漢字手書き練習Webアプリケーション「書きトレ」** です。
外部ビルドツール（Webpack / Vite 等）を介さず、ブラウザ標準の **ES Modules (`import` / `export`)** による疎結合なモジュール設計を採用しています。

### 核心設計思想

1. **Chromebook（1366×768）完全対応（スクロールゼロ設計）**:
   * アドレスバーやシェルフによる実効表示領域（約550〜600px）を前提とし、全体を `height: 100vh; overflow: hidden;` で固定。
   * 手書きキャンバス（250×250px）、リアルタイムプレビュー枠（52×52px）、上下判定カード（72×72px）により、縦スクロールを一切発生させません。
2. **インクルーシブ対応（利き手モードのオンデマンド切替）**:
   * 右上メニューの「🤚 きき手」ボタン、または初回ログイン時の自動ポップアップで設定可能。
   * CSS の `flex-direction: row-reverse;` により、手書き描画領域を画面左側、問題提示を画面右側へと瞬時に反転。書く手で問題文が隠れるのを防ぎます。
   * 設定はクラウド（GAS `users`シート）およびローカルキャッシュに即時保存され、次回以降も自動復元されます。
3. **ゼロ秒レスポンス UX（事前先読み / プリフェッチ）**:
   * 起動時は静的 JSON (`data/users.json`) によりミリ秒単位でクラス・名前プルダウンを描画。
   * 操作中の数秒間にバックグラウンドで Google Apps Script (GAS) 経由で認証情報・進捗サマリーを非同期先読み。ログインボタン押下時は通信待ちゼロ（0ミリ秒）で即時認証。
   * `localStorage` によるセッション保持を備え、2回目以降はログインモーダルを自動スキップ。
4. **引き継ぎ・異動に強い「2ファイル分離型」クラウドデータ基盤**:
   * **マスター・進捗ファイル**: 児童数分の固定行（1人1行）。認証照合・進捗サマリー・設定（利き手/PIN）を高速読み書き。
   * **詳細ログファイル**: 追記専用（Append-only）。1問ごとのストローク数・誤答文字・タイムスタンプを蓄積。画面表示からは一切読み出さないため、ログが数万行に膨らんでも日常利用の速度低下が起きません。
5. **自己調整学習を促すリアルタイム文字プレビュー**:
   * 熟語や送り仮名の練習中、書いた文字が下部のプレビュー枠へリアルタイムにDataURL（画像）として同期・永続表示。
   * 1文字ごとのバランスや完成形を1画面で俯瞰して確認した上で、独立した大きな「答え合わせ！」ボタンを押下可能。

---

### ディレクトリ構成

```text
kanji_practice_app/
│
├── index.html                    # ログイン/各種モーダル/メニュー/練習の骨格DOM
├── css/
│   └── style.css                 # 2カラムレイアウト・利き手反転・ボタン状態・プレビュー
├── data/
│   ├── users.json                # 静的児童名簿（クラス・出席番号・ひらがな名・userId）※PIN非保持
│   └── grade5_questions.json     # 小学5年生 全44回（計220問・各5問）構造化データ
├── assets/
│   ├── images/
│   │   └── logo_01.png           # 「書きトレ」公式ブランドロゴ画像
│   └── audio/                    # 効果音アセット (MP3)
│       ├── correct.mp3           # 正解音 (ピンポン♪)
│       ├── wrong.mp3             # 不正解音 (ブブー)
│       └── complete.mp3          # 全問クリア音 (ファンファーレ)
└── js/
    ├── main.js                   # アプリ統括・認証/設定フロー・プレビュー同期・統合判定
    ├── canvas.js                 # 250px手書き描画・アンドゥ/リドゥ・ストローク管理
    ├── recognition.js            # Google Input Tools 手書き文字認識 API 連携（第2候補まで）
    ├── audio.js                  # Web Audio API によるプリロード & 自動再生ガード
    ├── kanjivg.js                # 教科書体SVG直接描画 & 筆順アニメーション再生
    ├── ui.js                     # 画面DOM描画・利き手制御・ボタン活性制御
    └── logger.js                 # GAS Web API 通信（先読み・進捗保存・利き手/PIN更新）

```

---

## 2. クラウドデータ仕様（Google スプレッドシート ＆ GAS）

### 1. スプレッドシート構成

* **ファイルA: `漢字アプリ_ユーザー・進捗マスター**`
* `users` シート: 名簿・PIN・設定情報
* A列: `userId`
* B列: `className`
* C列: `studentNo`
* D列: `kanaName`
* E列: `pin`（暗証番号4桁）
* F列: `handMode`（`left` または `right`。未設定時は空文字）


* `progress` シート: 画面表示用サマリー（1児童1行）
* A列: `userId`
* B列: `clearedSets` (JSON配列: 例 `["1学期_01", "1学期_02"]`)
* C列: `weakChars` (JSON連想配列: 例 `{"初": 2, "複": 1}`)
* D列: `lastLogin` (更新日時)




* **ファイルB: `漢字アプリ_詳細ログ**`
* `logs` シート: 書き込み専用履歴（1問1行）
* A列: `timestamp` | B列: `userId` | C列: `setId` | D列: `qIndex` | E列: `isSuccess` | F列: `detailJson`





### 2. GAS Web API (`backend.gs`) エンドポイント機能

* `prefetchAllData`: 起動時の事前先読み用。`users` と `progress` を結合した連想配列を返却。
* `saveProgressAndLog`: 単元クリア時に `progress` タブの1行上書き更新 ＋ `logs` タブへの一括追記。
* `updateHandMode`: 利き手設定を `users` シートの F列へ即時書き込み。
* `updatePin`: 変更された暗証番号を `users` シートの E列へ即時書き込み。

> **デプロイ時のお作法**:
> コードを変更した際は、必ず **「デプロイを管理」➔「鉛筆アイコン」➔「新バージョン」** を選択してデプロイしてください。アクセスできるユーザーは必ず **「全員（Anyone）」** に設定します。

---

## 3. UI/UX 仕様詳細（v4 最新版）

### 1. ログイン画面

* カード最上部にブランドロゴ（`logo_01.png`）を中央配置。
* 入力欄は「① クラス」「② なまえ」「③ パスワード（すうじ4けた）」の3ステップ。
* 「なまえ」はクラス選択前は「なまえを えらんでね」でグレーアウト。選択肢は番号を除き「ひらがな氏名」のみ表示。
* 認証ボタン表記は「ログインする」。

### 2. メニュー画面

* **ヘッダー**: 左側に「書きトレ」ロゴ（幅165px）、右側にユーザー情報バー（クラス・名前・`[🤚 きき手]` `[🔑 パスワード]` `[ログアウト]` の3ボタン）を横並び配置。
* **学年選択**: 「1. どの がくねんの かんじ？」（小学5年生）。
* **単元選択**: 「2. もんだいを えらぶ」。学期タブは「1学期」「2学期」「3学期（準備中）」。
* **単元ボタン**: 「その1」「その2」... の表記で横8列グリッド。24回分が3行で収まりスクロール不要。クリア済み単元には右上に「💮」バッジを表示。
* **スタートボタン**: 「書き取りにチャレンジ！」。

### 3. 回答（練習）画面

* **左カラム（問題ペイン）**:
* 幅540pxに拡張し、問題文を「左寄せ」かつフォントサイズ `1.65rem` に設定。不要な改行を防止。
* ヘッダーに「書きトレ」ロゴ（幅165px）と「もどる」ボタンを配置。
* 下部にマスコットプレースホルダー（✏️枠）と、発話を表す「吹き出し（三角しっぽ付き）」を配置。


* **右カラム（描画ペイン）**:
* 履歴ボタン: 矢印マークを廃止し、改行なし1行の `[1画もどす]` `[1画すすむ]` ボタンをキャンバス両脇に配置。
* 操作ボタン: **`[前の文字へ]`（青） ➔ `[この文字を消す]`（橙） ➔ `[次の文字へ]`（青）** の固定3連ボタン。
* `前の文字へ`: 1文字目はグレーアウト。
* `この文字を消す`: 0画のときはグレーアウト。
* `次の文字へ`: 末尾文字または送り仮名上限時はグレーアウト。


* **リアルタイムプレビュー枠**:
* 書いた文字の見た目を画像（DataURL）として永続保持。文字を移動しても消えず、全文字の筆跡が下部に並ぶ。枠タップによる文字移動も可能。


* **独立型「答え合わせ！」ボタン**:
* プレビュー枠の直下に全幅のメイン緑色ボタンとして独立配置。必要な文字がすべて書かれるまで安全にグレーアウト。





---

## 4. 今後の開発・拡張ロードマップ

* [ ] **マスコットキャラクターの正式実装**:
* 左下のプレースホルダー（✏️）を専用イラスト画像に差し替え、正答・誤答に応じた表情切り替え。


* [ ] **学習記録ダッシュボードの追加**:
* 児童向け「がくしゅうきろく」画面。`progress` の苦手文字（`weakChars`）や `logs` の推移表示。


* [ ] **他学年データ（1〜4年・6年）の拡充**:
* `data/gradeX_questions.json` の追加と学年プルダウンの連携。



```